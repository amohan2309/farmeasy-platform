server:
  port: 8085

spring:
  application:
    name: chatbot-service

farmeasy:
  subscription-service-url: ${SUBSCRIPTION_SERVICE_URL:http://localhost:8082}
  llm:
    provider: ${LLM_PROVIDER:mock}
    anthropic-api-key: ${ANTHROPIC_API_KEY:}
    model: ${LLM_MODEL:claude-sonnet-4-20250514}
